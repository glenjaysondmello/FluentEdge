import 'package:flutter/material.dart';

class AppColors {
  static const backgroundStart = Color(0xFF2A2A72);
  static const backgroundEnd = Color(0xFF009FFD);
  static const card = Color(0x22FFFFFF);
  static const text = Colors.white;
  static const textFaded = Color(0xAAFFFFFF);
  static const accent = Color(0xFF00D2FF);
  static const success = Color(0xFF39FF14);
  static const error = Color(0xFFFF4081);
  static const recording = Colors.redAccent;
}
